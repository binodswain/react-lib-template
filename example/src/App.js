import React from 'react';
import ReactPackage from "react-lib-template";
import './App.scss';


function App() {
  return (
      <div className="example">
          <ReactPackage />
          <p>If you see h1 tag in white color with #5000ca bgColor, the app is running fine.</p>
          <p>Now modify the package as per requirement.</p>
      </div>
  );
}

export default App;
